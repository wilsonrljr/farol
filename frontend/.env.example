# Vite lê esta variável ao iniciar o servidor de desenvolvimento ou durante o
# build; ela não pode ser alterada depois em um container já construído.
# Desenvolvimento local (backend na porta 8000):
VITE_API_BASE=http://localhost:8000
# Em produção, omita/deixe vazio para usar /api no mesmo domínio via Nginx.
