"""Domain layer (business objects).

This package defines internal, framework-agnostic representations of simulation
outputs. The API layer (Pydantic/FastAPI) should map these objects to response
models at the boundary.
"""
